{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "FocusNet: Sunucu + Edge (önce npm start)",
      "type": "msedge",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    },
    {
      "name": "FocusNet: Sunucu + Chrome",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:3000",
      "webRoot": "${workspaceFolder}"
    }
  ]
}
